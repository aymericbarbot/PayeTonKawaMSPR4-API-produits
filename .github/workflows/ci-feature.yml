name: ci-feature-produits      # tests unitaires + rollback si push direct

on:
  # Push sur n'importe quelle branche SAUF main et dev
  push:
    branches-ignore: [main, dev]

  # Pull-request dont la target est dev
  pull_request:
    branches: [dev]

permissions:
  contents: write
  id-token: write

jobs:
  unit:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.outcome.outputs.state }}
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm

    - run: |
        npm ci
        npm run build
        npm run test:unit
      id: test
      continue-on-error: true        # on capture succès/échec

    - id: outcome
      run: echo "state=${{ steps.test.outcome }}" >> $GITHUB_OUTPUT

  # Envoi d'email & rollback si échec — uniquement pour push (pas PR)
  notify-rollback:
    needs: unit
    if: github.event_name == 'push' && needs.unit.outputs.result == 'failure'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 2               # pour remonter d'un commit

    - name: Send failure email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.SMTP_SERVER }} # SMTP server
        server_port:     ${{ secrets.SMTP_PORT }}
        username:        ${{ secrets.SMTP_USER }}
        password:        ${{ secrets.SMTP_PASS }}
        subject: "[CI Produits] Tests KO sur la branche ${GITHUB_REF_NAME}"
        to: ${{ github.actor }}@example.com
        from: ${{ secrets.SMTP_FROM }}
        body: |
          Les tests unitaires ont échoué pour le commit ${{ github.sha }}.
          La branche a été rollback.
          Consulte l’action ci-feature-produits pour les logs.

    - name: Rollback feature branch
      run: |
        git config user.email "bot@github"
        git config user.name  "github-actions"
        git reset --hard HEAD~1
        git push --force origin HEAD:${GITHUB_REF_NAME}
